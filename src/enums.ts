export enum ColumnTypes {
    STRING = 'string',
    NUMBER = 'number',
    BINARY = 'binary',
    BOOLEAN = 'boolean',
    DATE = 'date',
    OBJECT = 'object',
    ARRAY = 'array',
    NULL = 'null',
    STRING_SET = 'string_set',
    NUMBER_SET = 'number_set',
    BINARY_SET = 'binary_set',
}
